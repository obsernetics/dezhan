--  Platform storage layer (Ada, not part of the SPARK trusted core). Uses the
--  verified SHA-256 from the trusted core for content addressing and integrity.
package Dezhan.Storage with SPARK_Mode => Off is
end Dezhan.Storage;
