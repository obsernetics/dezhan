with "../dezhan_trusted_core.gpr";

project Tests is

   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "obj";
   for Main use ("test_retention.adb", "test_clock_guard.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2022", "-gnatwa", "-gnata", "-g");
   end Compiler;

end Tests;
